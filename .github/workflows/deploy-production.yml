name: Deploy Production

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release tag from staging (example: v25.3.0-staging)'
        required: true
        type: string

jobs:
  deploy_production:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout selected tag
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.release_tag }}

      - name: Show selected release
        run: |
          echo "🔥 Deploying release: ${{ inputs.release_tag }}"

      - name: Build production image
        run: |
          docker build -t test-app:production .

      - name: Deploy to PRODUCTION
        run: |
          echo "🚀 Deploying ${{ inputs.release_tag }} to PRODUCTION"
          echo "Add production deployment logic here"
